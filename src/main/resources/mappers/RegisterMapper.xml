<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="RegisterMapper">

    <insert id="insertAdminRegister" parameterType="com.jj.jejuTour.Register.vo.RegisterVo">
        INSERT INTO admin_info
        (
          admin_id
          , admin_pwd
          , admin_name
          , login_fail_count
          , create_dt
        )
        VALUES
        (
          #{admin_id}
          , #{admin_pwd}
          , #{admin_name}
          , 0
          , NOW()
        )
    </insert>

</mapper>